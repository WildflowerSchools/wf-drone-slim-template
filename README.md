# wf-drone-slim-template

Simple template for experimenting with drone-slim in the drone pipeline

## Steps

The repo must be "secure" in order to mount host volumes. Use the drone-cli:

`drone repo update --trusted <<repo>>`
